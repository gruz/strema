# Config file with sensitive data (generated by install.sh)
config/stream.conf*
!config/stream.conf.template

# Log files directory (but keep the directory structure)
logs/*.log
logs/*.log.old
*.log
*.log.old

# VERSION file (generated by GitHub Action during release)
VERSION

# Service manager (generated from template during install)
scripts/service_manager.sh

__pycache__
.cache