# Release Process

## Створення нового релізу

### 1. Підготовка

Переконайтесь, що всі зміни закомічені:
```bash
git status
git add .
git commit -m "Your changes description"
```

### 2. Створення релізу

**Stable release:**
```bash
# Створіть тег з версією
git tag v0.1.1

# Запуште тег на GitHub
git push origin master
git push origin v0.1.1
```

**Beta release:**
```bash
# Створіть тег з суфіксом -beta
git tag v0.2.0-beta.1

# Запуште тег на GitHub
git push origin master
git push origin v0.2.0-beta.1
```

### 3. GitHub Action автоматично:

1. Витягне версію з тега
2. Створить файл `VERSION`
3. Створить архів `strema-v0.1.1.tar.gz`
4. Згенерує checksums
5. Створить changelog з commit messages
6. Опублікує Release на GitHub

### 4. Редагування changelog (опціонально)

Після створення релізу ви можете:
1. Перейти на https://github.com/gruz/strema/releases
2. Знайти свій реліз
3. Натиснути "Edit release"
4. Відредагувати опис
5. Зберегти

## Встановлення

### Перше встановлення (без git)

**Остання stable версія:**
```bash
curl -fsSL https://raw.githubusercontent.com/gruz/strema/master/install.sh | sudo bash
```

**Конкретна версія:**
```bash
curl -fsSL https://raw.githubusercontent.com/gruz/strema/master/install.sh | sudo bash -s v0.1.0
```

### Встановлення для розробників (з git)

```bash
git clone https://github.com/gruz/strema.git
cd strema
sudo ./install.sh
```

## Оновлення

### Через веб-інтерфейс

1. Відкрийте http://IP:8081
2. Розділ "🔄 Оновлення системи"
3. Оберіть версію
4. Натисніть "Оновити"

### Через CLI

```bash
sudo ~/strema/scripts/update.sh v0.1.1
```

### Перевірка доступних оновлень

```bash
~/strema/scripts/check_updates.sh stable
# або
~/strema/scripts/check_updates.sh beta
```

## Канали оновлень

- **stable** - стабільні релізи (без суфіксів)
- **beta** - експериментальні релізи (з суфіксом `-beta`, `-alpha`, `-rc`)

## Структура версій

- `v0.1.0` - stable release
- `v0.2.0-beta.1` - beta release
- `v0.2.0-rc.1` - release candidate

## Що зберігається при оновленні

✅ Зберігається:
- `config/stream.conf` - конфігурація
- `logs/` - логи

❌ Видаляється:
- Всі інші файли (замінюються новою версією)

## Troubleshooting

### Якщо оновлення не вдалося

1. Перевірте логи:
```bash
sudo journalctl -u forpost-stream-web -n 50
```

2. Спробуйте оновити вручну:
```bash
sudo ~/strema/scripts/update.sh v0.1.0
```

3. У крайньому випадку - переінсталюйте:
```bash
curl -fsSL https://raw.githubusercontent.com/gruz/strema/master/install.sh | sudo bash -s v0.1.0
```

### Перевірка поточної версії

```bash
cat ~/strema/VERSION
```

## GitHub Actions

Workflow знаходиться в `.github/workflows/release.yml`

Він запускається автоматично при push тега `v*`.

Переконайтесь, що у вашому репозиторії:
- ✅ Actions увімкнені (Settings → Actions → Allow all actions)
- ✅ Workflow має права на створення релізів (автоматично)
